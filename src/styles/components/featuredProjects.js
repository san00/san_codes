import styled from "styled-components"
import { spacingScale } from "../utils"

const ColourWrapper = styled.section`
  background: #fcfcfc;
  padding: ${spacingScale.spacing_l};
`

export default ColourWrapper
